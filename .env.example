# Where the authenticated dashboard is hosted (Fly.io app).
# Used to redirect /login, /sign-up, /logout, /dashboard CTAs.
NEXT_PUBLIC_DASHBOARD_URL=https://app.cruxlogic.ai

# Supabase — required for the /blog pages and the /api/feedback route.
# Use the SAME project as the dashboard (read-only usage here).
NEXT_PUBLIC_SUPABASE_URL=https://kmycsuojnaxnzhljslmt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
